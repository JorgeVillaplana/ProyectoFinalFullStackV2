html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title Confirmación de reserva
    body
        h3=`Hola, ${name}.` 
        h3=`Tu reserva para el free tour con Explora Toledo ha sido `
        h2=`Confirmada.`
        P=`Te esperamos el ${date} a las ${hour}`
